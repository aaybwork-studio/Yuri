---
phase: 5
slug: homepage-layout-reform
status: approved
shadcn_initialized: false
preset: none
created: 2026-06-11
reviewed_at: 2026-06-11
---

# Phase 5 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source / Notes |
|----------|-------|----------------|
| Tool | none | Shopify Liquid context; shadcn is not applicable |
| Preset | not applicable | N/A |
| Component library | none | Native Liquid templates |
| Icon library | Native Dawn SVGs | Dawn theme icon assets (`snippets/icon-*.liquid`) |
| Font | Space Grotesk & Hanken Grotesk | Pre-populated from `entry.css` / Phase 1 tokens |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding (minimum threshold) |
| xl | 32px | Layout gaps |
| 2xl | 48px | Page width mobile padding, major section breaks |
| 3xl | 64px | Page-level spacing (default section padding) |

Exceptions:
- **Navbar Dot Separators**: `8px` (0.8rem) margin between links.
- **Page-level width padding (desktop)**: `48px` (4.8rem) padding on desktop for `.page-width` to ensure grid alignment.
- **Dynamic Section Padding**: Top/bottom padding range up to `128px` (which is a multiple of 4) is configurable in Liquid schema settings to support custom Zen layouts.

---

## Typography

| Role | Size | Weight | Line Height | Font Family | Source / Notes |
|------|------|--------|-------------|-------------|----------------|
| Body | 16px | 300 (Light) | 1.5 | Hanken Grotesk | General content, section descriptions |
| Label | 12px | 500 (Medium) | 1.2 | Hanken Grotesk / Space Grotesk | Navbar navigation links, product details, buttons |
| Heading | 24px | 500 (Medium) | 1.2 | Space Grotesk | Product card titles, block headings |
| Display | 48px | 300 (Light) | 1.1 | Space Grotesk | Hero title, split panel section headers |

---

## Visual Hierarchy and Focal Point

- **Primary Focal Point**: The full-screen slideshow hero image and large display headings (`Space Grotesk`, `48px`, `300 (Light)`).
- **Accessibility Labels**: Icon-only navigation buttons in the header (search, wishlist star, cart) must render with screen-reader friendly `aria-label` text (e.g. `aria-label="search"`, `aria-label="wishlist"`, `aria-label="cart"`).
- **Hierarchy Scale**:
  1. **Anchor**: Full-screen slide image and display titles (`48px`).
  2. **Page sections**: Block headings (`24px`).
  3. **Product details/UI buttons**: Labels, price tags, and custom buttons (`12px`).
  4. **Supportive text**: Description paragraphs (`16px`).

---

## Color

| Role | Value | Usage | Source / Notes |
|------|-------|-------|----------------|
| Dominant (60%) | `#eeecd3` | Base body background (`--color-brand-bg`), transparent ivory surfaces | Warm Sand White |
| Secondary (30%) | `#403002` | Dark brown (`--color-brand-text`), primary text, primary button backgrounds, header/footer text | Dark Brown |
| Secondary (30%) | `#2C3A2E` | Grounded Forest Green (`--color-forest`), solid card backgrounds, custom panels | Grounded Forest |
| Accent (10%) | `#efbf04` | Gold (`--color-accent-gold`), active wishlist indicator, focus outlines | Gold |
| Accent (10%) | `#cc1574` | Rose (`--color-accent-rose`), sale badge highlights, promo tags | Rose |
| Destructive | `#cc1574` | Warning messages, error states | Rose |

Accent reserved for:
- **Gold (`#efbf04`)**: Active wishlist star icon states, focus outlines, selected navigation dots.
- **Rose (`#cc1574`)**: Sale badges, product highlight badges, error message text.

---

## Copywriting Contract

| Element | Copy | Usage |
|---------|------|-------|
| Primary CTA | "explore collection" | Main slideshow hero & featured product section CTA links |
| Empty state heading | "no products found" | Empty collection grid or carousel fallback |
| Empty state body | "no products match this criteria. please clear filters and try again." | Detail description for empty state |
| Error state | "unable to load products. please refresh the page." | Dynamic product fetch errors |
| Destructive confirmation | none | No destructive actions in Phase 5 scope |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not required |
| none | none | not applicable |

---

## Phase-Specific Layout & Interaction Contracts

### 1. Minimalist Navbar Reform (`sections/header.liquid`)
- **Logo Position**: Position logo on the right side of the navbar (`logo-right`).
- **Navigation Links**: Left/middle aligned, lowercase, dot-separated (e.g. `home • about • products`).
- **Icon Group**: Group search, star (wishlist), and cart icons inline with links. No button outlines or vertical border separators.
- **Hover Transitions**: Remove the legacy double-text-slide translate hover animation on links. Use a static color transition for modern simplicity.

### 2. Full-Screen Slideshow Hero (`sections/custom-hero.liquid`)
- **Dimensions**: Hardcode container height to `100vh` / `100dvh` for a full-screen immersive display.
- **Text Alignment**: Support dynamic Left, Center, Right alignment for slide text overlay blocks.
- **Backdrop Overlay**: Add a schema range slider (0% to 90% opacity in steps of 10%) for a subtle dark background overlay.

### 3. Spacing & Border Controls (All Custom Sections)
- **Top/Bottom Padding**: Expose top/bottom padding controls in the schema ranging from 24px to 128px (step size 8px).
- **Section Dividers**: Add a toggle checkbox to show/hide a thin bottom border (`border-b zen-border` at 10% opacity of the text color).

### 4. Carousel/Grid & Splits (`sections/custom-featured-products.liquid`)
- **Layout Mode Toggle**: Add schema option to switch between a horizontal scroll carousel and a static responsive grid.
- **Desktop Columns**: Allow selecting 3 or 4 columns for desktop.
- **Split Header Option**: Support splitting layout: section title/description on the left (1/3 width), product carousel/grid on the right (2/3 width).
- **Contrast Cards**: Support solid dark background card container style (color `#403002` or `#2C3A2E`) to showcase light/white product packaging.
- **Mirrored Split Panel Ratios**: Support select splits (50/50, 60/40, 40/60) for mirrored panels (`sections/custom-browse-mirrored.liquid`).

---

## Checker Sign-Off

- [x] Dimension 1 Copywriting: PASS
- [x] Dimension 2 Visuals: PASS
- [x] Dimension 3 Color: PASS
- [x] Dimension 4 Typography: PASS
- [x] Dimension 5 Spacing: PASS
- [x] Dimension 6 Registry Safety: PASS

**Approval:** approved 2026-06-11
